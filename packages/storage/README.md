# @pwmnger/storage

Persistence layer for PwmngerTS.

## Features
- **Cross-Platform Storage:** Abstracted storage API using `localStorage`, `chrome.storage.local`, and `AsyncStorage` (Mobile).
- **Vault Persistence:** Securely save and load encrypted vault blobs.
- **Session Management:** Handle JWT and session metadata locally.
